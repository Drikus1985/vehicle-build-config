# Optional integrations. The app runs fully local-first without any of these.
# No secrets are ever required in the client bundle.

# Base URL for a future persistence backend (builds, share links). Unset = local IndexedDB only.
# VITE_BACKEND_URL=

# Base URL for a real Titanforge fabrication API. Unset = local adapter (IndexedDB).
# VITE_TITANFORGE_API_URL=
